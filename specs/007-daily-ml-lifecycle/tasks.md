---

description: "Задачи фичи: жизненный цикл Daily ML и план портфеля"
---

# Tasks: Жизненный цикл Daily ML и план портфеля

**Input**: Design documents from `/specs/007-daily-ml-lifecycle/`

**Prerequisites**: [plan.md](./plan.md), [spec.md](./spec.md), [research.md](./research.md),
[data-model.md](./data-model.md), [contracts/](./contracts/)

**Tests**: включены. Принцип IV конституции требует сопровождать новую существенную логику
тестами; в проекте это сложившаяся практика.

**Organization**: задачи сгруппированы по пользовательским историям. US1–US3 составляют
работающий цикл, US4 делает его видимым, US5 доводит до плана.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: можно выполнять параллельно (разные файлы, нет незакрытых зависимостей)
- **[Story]**: к какой истории относится задача (US1–US5)
- В описании — точный путь к файлу

## Path Conventions

`backend/src/financial_ai/`, `backend/tests/`, `daily-ml-emulator/src/`, `frontend/src/`,
`frontend/tests/`. Раскладка существующая.

Артефакт Open Design: `daily-ml.html`, `portfolio-plan.html`, `market-data-design.md`,
`design-assets/`.

---

## Phase 1: Setup — артефакты правятся раньше кода

**Purpose**: привести спецификации прошлых фич в соответствие тому, что от них потребуется,
и перенести оформление из утверждённого дизайна. Принцип III.

- [X] T001 [P] Дополнить `specs/002-daily-ml-emulator/contracts/daily-ml-emulator-api.md`: в ответе появляется `model_version`, описывается настраиваемая задержка ответа и её назначение — сделать состояние «выполняется» наблюдаемым
- [X] T002 [P] Дополнить `specs/005-market-data-control/contracts/coverage-report.md`: справочник активов пополняется размером лота; на состав сводки и на готовность данных это не влияет
- [X] T003 [P] Дополнить `docs/daily-ml-model.md` разделом о том, что лежит **над** моделью: правила §10.1 и §10.3 исследовательского репозитория, разрыв между весами и исполнением, и почему план считает платформа
- [X] T004 Перенести стили разделов «Ранжирование» и «План портфеля» из артефакта в `frontend/src/app/styles/design.css` (`design-assets/daily-ml.css` и правила плана), с сохранением имён классов и порядка

**Checkpoint**: спецификации описывают то, что будет реализовано; стили артефакта в проекте.

---

## Phase 2: Foundational — без этого ни одна история не работает

**⚠️ CRITICAL**: до завершения фазы работа по историям не начинается.

- [X] T005 Создать миграцию `backend/migrations/versions/0010_daily_ml_runs.py`: таблицы `daily_ml_run` и `daily_ml_ranking_item`, уникальный индекс `(asof_date, dataset_digest, model_id, model_version)`, индексы по `asof_date DESC` и `status`, колонка `lot_size` в `market_asset`
- [X] T006 [P] Описать ORM-модели в `backend/src/financial_ai/daily_ml/models.py` по `data-model.md`; скор — `NUMERIC(28,9)`, как денежные величины проекта
- [X] T007 [P] Реализовать `backend/src/financial_ai/daily_ml/repository.py`: создание задания с обработкой конфликта уникальности, выборка очереди, переходы состояний, запись результата одной транзакцией с переводом в `success`
- [X] T008 [P] Добавить ключ advisory-блокировки для обработчика ранжирования в `backend/src/financial_ai/sync/advisory.py` — тот же класс, другой номер объекта, чтобы блокировки были различимы в `pg_locks`
- [X] T009 [P] Добавить в эмулятор `DAILY_ML_EMULATOR_LATENCY_SECONDS` и `DAILY_ML_EMULATOR_MODEL_VERSION` в `daily-ml-emulator/src/daily_ml_emulator/config.py`; задержку перед ответом и версию в ответе — в `app.py`
- [X] T010 [P] Дополнить тесты эмулятора `daily-ml-emulator/tests/test_api.py`: версия модели в ответе, задержка соблюдается, границы эмулятора не расширились
- [X] T011 Добавить два маршрута и четвёртый пункт навигации в `frontend/src/app/router/router.tsx` и `frontend/src/widgets/app-header/AppHeader.tsx`; заготовки страниц `frontend/src/pages/daily-ml/DailyMlPage.tsx` и `frontend/src/pages/portfolio-plan/PortfolioPlanPage.tsx`

**Checkpoint**: схема готова, эмулятор проходит состояния по-настоящему, разделы открываются по адресу.

---

## Phase 3: User Story 1 — ранжирование считается само (Priority: P1) 🎯 MVP

**Goal**: после сбора данных система сама материализует набор, запрашивает ранжирование и
сохраняет результат.

**Independent Test**: дождаться закрытия сессии на стенде и убедиться, что прогон появился
сам, прошёл состояния и сохранил результат (quickstart С6–С7).

### Тесты

- [X] T012 [P] [US1] Тесты выполнения прогона в `backend/tests/daily_ml/test_runner.py`: создание задания по готовой дате, переходы `queued → running → success`, сохранение результата, обновление последней успешной даты
- [X] T013 [P] [US1] Тесты готовности в `backend/tests/daily_ml/test_readiness.py`: дата с пропуском внутри требуемого окна не готова; необязательная группа на готовность не влияет; неполный набор останавливает запуск

### Реализация

- [X] T014 [US1] Реализовать `backend/src/financial_ai/daily_ml/readiness.py`: дешёвая проверка по пропускам и незакрытым источникам обязательных групп, окончательная — по объявлению полноты собранного набора (research.md R5)
- [X] T015 [US1] Добавить перечень обязательных групп в `backend/src/financial_ai/config.py` (`DAILY_ML_REQUIRED_DATA_GROUPS`) с умолчанием, не включающим дивиденды
- [X] T016 [US1] Реализовать `backend/src/financial_ai/daily_ml/runner.py`: взятие задания под advisory-блокировкой, сборка набора, запрос ранжирования, сохранение результата, перевод в `success` или `failed`
- [X] T017 [US1] Реализовать очистку причины отказа в том же модуле: короткая человекочитаемая фраза вместо текста исключения, подробности остаются в журнале — по образцу `market_data/runner.py`
- [X] T018 [US1] Реализовать `backend/src/financial_ai/daily_ml/scheduler.py`: периодическая проверка, вызов реконсиляции, обработка очереди по одной дате
- [X] T019 [US1] Подключить планировщик ранжирования в `backend/src/financial_ai/worker/app.py` и вызвать реконсиляцию после успешного ежедневного сбора в `backend/src/financial_ai/market_data/scheduler.py`
- [X] T020 [US1] Тест отказа звена в `backend/tests/daily_ml/test_runner.py`: недоступный эмулятор даёт `failed` с пригодной для показа причиной, собранные данные не изменяются

**Checkpoint**: цикл замкнулся — ранжирование считается и сохраняется без участия человека.

---

## Phase 4: User Story 2 — данные доводятся до последней закрытой сессии (Priority: P1)

**Goal**: граница данных движется вперёд сама, глубокий догон при этом не запускается.

**Independent Test**: остановить сбор на несколько сессий, запустить и убедиться, что данные
доведены до последней закрытой сессии (quickstart С1–С2, С5).

### Тесты

- [X] T021 [P] [US2] Тесты доведения данных в `backend/tests/market_data/test_advance.py`: разрыв в пределах лимита восстанавливается; разрыв сверх лимита не собирается; незакрытая сессия не собирается
- [X] T022 [P] [US2] Тесты сбора размера лота в `backend/tests/market_data/test_securities.py`: лот попадает в справочник; отсутствие лота не делает дату неготовой; доска подставляется только в адрес своего раздела

### Реализация

- [X] T023 [US2] Реализовать `backend/src/financial_ai/market_data/advance.py`: синхронизация календаря, определение последней закрытой сессии, сбор недостающих сессий существующим `ingest_session` в пределах лимита
- [X] T024 [US2] Добавить `MARKET_DATA_STARTUP_RECOVERY_MAX_SESSIONS` в `backend/src/financial_ai/config.py` с разумным умолчанием
- [X] T025 [P] [US2] Реализовать источник размера лота `backend/src/financial_ai/market_data/sources/securities.py`: текущее состояние бумаг доски TQBR, поле `LOTSIZE`
- [X] T026 [P] [US2] Добавить построитель адреса раздела бумаг акций в `backend/src/financial_ai/market_data/iss/urls.py` по образцу `futures_securities_url`
- [X] T027 [US2] Подключить источник лота к группе справочников в `backend/src/financial_ai/market_data/groups.py` и к циклу сбора в `ingest.py`
- [X] T028 [US2] Записать происхождение нового источника в `backend/src/financial_ai/market_data/PROVENANCE.md`: откуда берётся, что именно перенесено, что нет

**Checkpoint**: система не остаётся на старой границе данных после простоя.

---

## Phase 5: User Story 3 — восстановление после перезапуска без повторов (Priority: P1)

**Goal**: перезапуск не приводит ни к повторному сбору, ни к повторному ранжированию.

**Independent Test**: перезапустить систему в разных состояниях и убедиться, что повторной
работы нет, а прерванное задание не висит (quickstart С3, С8–С9, С11).

### Тесты

- [X] T029 [P] [US3] Тесты идемпотентности в `backend/tests/daily_ml/test_idempotency.py`: успешный прогон не повторяется при перезапуске, тике, реконсиляции и ручной проверке; два одновременных создания дают одно задание
- [X] T030 [P] [US3] Тесты реконсиляции в `backend/tests/daily_ml/test_reconcile.py`: данные есть и ML отстаёт — выполняется только ML; разрыв сверх лимита не догоняется; после догона ставится только последняя готовая дата
- [X] T031 [P] [US3] Тесты планировщика рыночных данных в `backend/tests/market_data/test_scheduler.py`: повторный сбор после перезапуска не выполняется; неудачная попытка не блокирует повтор в тот же день

### Реализация

- [X] T032 [US3] Реализовать `backend/src/financial_ai/daily_ml/reconcile.py`: последняя закрытая сессия, отставание данных, отставание ранжирования, постановка заданий; глубокий догон не запускается
- [X] T033 [US3] Реализовать восстановление при старте в том же модуле: задания в состоянии `running` переводятся в `failed` с причиной «прервано перезапуском» и возвращаются в очередь (research.md R4)
- [X] T034 [US3] Перевести `backend/src/financial_ai/market_data/scheduler.py` на сохранённое состояние: решение принимается по успешному прогону сбора за последнюю закрытую сессию, отметка в памяти процесса удаляется
- [X] T035 [US3] Реализовать вычисление устаревания входа в `backend/src/financial_ai/daily_ml/readiness.py`: сравнение дайджеста прогона с дайджестом набора, который построился бы сейчас
- [X] T036 [US3] Реализовать правило пересчёта: последняя готовая дата с устаревшим входом ставится в очередь автоматически, исторические — нет (FR-024, FR-025)
- [X] T037 [US3] Подключить реконсиляцию при старте в `backend/src/financial_ai/worker/app.py` и после завершения догона в `backend/src/financial_ai/market_data/runner.py`

**Checkpoint**: система переживает перезапуск, ничего не повторяя и ничего не теряя.

---

## Phase 6: User Story 4 — человек видит и ведёт жизненный цикл (Priority: P2)

**Goal**: раздел «Ранжирование» — состояние, очередь, история, просмотр прогона, управление.

**Independent Test**: пройти по разделу в каждом состоянии и сверить показанное с ответом
сервера (quickstart С7, С12–С14).

### Тесты

- [X] T038 [P] [US4] Контрактные тесты в `backend/tests/contract/test_daily_ml_api.py`: состояние, история с отбором и страницами, просмотр прогона, реконсиляция при актуальном состоянии, повтор и его отказы, `worker_unavailable`
- [X] T039 [P] [US4] Тесты состояний раздела в `frontend/tests/daily-ml-states.test.tsx`: восемь состояний из `contracts/ui-states.md`
- [X] T040 [P] [US4] Тесты истории и просмотра прогона в `frontend/tests/daily-ml-history.test.tsx`: страницы, отбор, открытие прогона, маркировка эмуляции, удалённый набор, устаревший вход

### Реализация

- [X] T041 [US4] Реализовать внутренние маршруты управления в `backend/src/financial_ai/worker/routes/daily_ml.py`: состояние, реконсиляция, пауза, повтор
- [X] T042 [US4] Реализовать публичные маршруты в `backend/src/financial_ai/api/routes/daily_ml.py` по `contracts/daily-ml-lifecycle-api.md`; чтение истории — из хранилища, команды — передачей внутреннему каналу
- [X] T043 [US4] Зарегистрировать роутер в `backend/src/financial_ai/api/app.py`
- [X] T044 [US4] Реализовать расчёт следующей проверки и правила начала расчёта на стороне сервера: интерфейс не должен выводить их сам (FR-053, FR-054)
- [X] T045 [P] [US4] Создать сущность `frontend/src/entities/daily-ml/`: типы состояния, прогона и результата, запросы, ключи, опрос во время работы
- [X] T046 [US4] Реализовать виджет состояния и управления `frontend/src/widgets/daily-ml-state/DailyMlState.tsx` по разметке артефакта: две даты, объяснение, текущий прогон, ожидаемая длительность по истории
- [X] T047 [P] [US4] Реализовать виджет очереди `frontend/src/widgets/daily-ml-queue/DailyMlQueue.tsx`: перечень дат и счётный прогресс «обработано N из M»
- [X] T048 [P] [US4] Реализовать историю прогонов `frontend/src/widgets/daily-ml-history/DailyMlHistory.tsx`: таблица, отбор по состоянию, страницы, пустое состояние
- [X] T049 [US4] Реализовать просмотр прогона `frontend/src/widgets/daily-ml-history/RunDrawer.tsx`: вход, исход, результат, маркировка эмуляции, удалённый набор, устаревший вход
- [X] T050 [US4] Реализовать действия `frontend/src/features/daily-ml-control/`: пауза, возобновление, немедленная проверка, повтор; «проверить сейчас» не читается как принудительный пересчёт
- [X] T051 [US4] Собрать раздел в `frontend/src/pages/daily-ml/DailyMlPage.tsx`
- [X] T052 [US4] Дополнить общий баннер процесса `frontend/src/widgets/process-rail/ProcessRail.tsx`: догон и inference показываются независимо, ни один не вытесняет другой

**Checkpoint**: жизненный цикл виден и управляем из интерфейса.

---

## Phase 7: User Story 5 — план портфеля (Priority: P2)

**Goal**: ранжирование доводится до целевых позиций на текущем счёте.

**Independent Test**: посчитать план на стенде и сверить состав и веса с правилом
(quickstart С15–С20).

### Тесты

- [X] T053 [P] [US5] Тесты правил в `backend/tests/portfolio_plan/test_policies.py`: равные доли по top-k и диапазоны рангов 1–100 с весами `0,2403 / 0,1063 / 0,2039 / 0,4495`
- [X] T054 [P] [US5] Тесты арифметики в `backend/tests/portfolio_plan/test_plan.py`: количества кратны лоту; остаток округления остаётся деньгами; вес недоступного актива уходит в деньги, а не перераспределяется; комиссия применяется к обеим сторонам; облигации и денежный фонд не затрагиваются
- [X] T055 [P] [US5] Контрактные тесты в `backend/tests/contract/test_portfolio_plan_api.py`: перечень правил, расчёт, все отказы из `contracts/portfolio-plan-api.md`
- [X] T056 [P] [US5] Тесты раздела в `frontend/tests/portfolio-plan.test.tsx`: план, параметры расчёта, состояния недоступности, отсутствие действий исполнения

### Реализация

- [X] T057 [P] [US5] Реализовать правила распределения `backend/src/financial_ai/portfolio_plan/policies.py` со ссылкой на разделы исследования, из которых они взяты
- [X] T058 [US5] Реализовать расчёт `backend/src/financial_ai/portfolio_plan/plan.py`: расчётный капитал, целевые суммы, целые лоты, разница к текущим позициям, комиссия; `Decimal` на всём пути
- [X] T059 [US5] Реализовать отказы расчёта в том же модуле: нет ранжирования, устарело, счёт не подключён, снимок устарел, активов меньше требуемого, нет цены или лота — каждый со своим кодом
- [X] T060 [US5] Реализовать публичные маршруты `backend/src/financial_ai/api/routes/portfolio_plan.py` и зарегистрировать их в `backend/src/financial_ai/api/app.py`
- [X] T061 [P] [US5] Создать сущность `frontend/src/entities/portfolio-plan/`: типы плана и правил, запрос расчёта
- [X] T062 [US5] Реализовать параметры расчёта `frontend/src/features/plan-settings/`: правило, лимит распределения, комиссия, валидация ввода
- [X] T063 [US5] Реализовать таблицу плана `frontend/src/widgets/position-plan/PositionPlan.tsx`: целевые позиции, разница, источник цены с датой сессии, неизменяемые инструменты, исключённые активы
- [X] T064 [US5] Собрать раздел в `frontend/src/pages/portfolio-plan/PortfolioPlanPage.tsx`; убедиться, что действий исполнения в разделе нет (FR-063)

**Checkpoint**: ранжирование доведено до практического ответа, портфель при этом не меняется.

---

## Phase 8: Polish & Cross-Cutting Concerns

- [X] T065 [P] Актуализировать `README.md`: жизненный цикл, доведение данных, план портфеля, новые переменные окружения
- [X] T066 [P] Актуализировать `AGENTS.md` и `docs/architecture.md`: новые публичные маршруты, место оркестрации, расчёт плана в `backend-api`
- [X] T067 [P] Добавить новые переменные в `deployments/docker-compose/docker-compose.yml`: задержка и версия эмулятора, предел восстановления, перечень обязательных групп, предел попыток
- [X] T068 Сверить реализацию с артефактом Open Design по `specs/007-daily-ml-lifecycle/contracts/ui-states.md` — **по `data-od-id`**, а не по классам; зафиксировать отступления с причиной
- [X] T069 Проверить доступность и адаптивность обоих разделов: 44 px, Escape и возврат фокуса, `prefers-reduced-motion`, ширина от 360 px, состояние текстом
- [X] T070 Пройти сценарии `specs/007-daily-ml-lifecycle/quickstart.md` на поднятом стенде, включая С9 (перезапуск во время прогона), С11 (догон не порождает лавину) и С21 (внутренний канал снаружи недоступен)
- [X] T071 Прогнать `scripts/check.sh` целиком, включая сборку образов

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: без зависимостей. T004 не блокирует backend-задачи
- **Foundational (Phase 2)**: зависит от Phase 1 — **блокирует все истории**
- **US1 (Phase 3)**: зависит только от Phase 2. Даёт работающий цикл
- **US2 (Phase 4)**: зависит только от Phase 2; с US1 не пересекается по файлам, кроме подключения в `ingest.py`
- **US3 (Phase 5)**: опирается на runner и readiness из US1 — выполняется после Phase 3
- **US4 (Phase 6)**: опирается на прогоны, которые создаёт US1, и на реконсиляцию из US3
- **US5 (Phase 7)**: опирается на сохранённые ранжирования (US1) и размер лота (US2)
- **Polish (Phase 8)**: после всех историй

### Внутри истории

- Тесты пишутся до реализации и сначала падают
- Модели и репозиторий — до логики
- Логика — до маршрутов
- Маршруты — до интерфейса
- Реализация — до сверки с артефактом

### Parallel Opportunities

- T001–T003 — три разных документа, параллельны
- T006–T010 — модели, репозиторий, блокировка и эмулятор не пересекаются
- T012–T013, T021–T022, T029–T031 — тестовые файлы независимы
- T025–T026 — источник и построитель адреса
- T045, T047, T048 — разные модули фронтенда
- T053–T056 — четыре независимых тестовых файла
- T065–T067 — три разных документа
- **US1 и US2 можно вести параллельно** сразу после Phase 2

---

## Parallel Example: Phase 2

```bash
# Не пересекающиеся модули основания:
Task: "ORM-модели в backend/src/financial_ai/daily_ml/models.py"
Task: "Репозиторий в backend/src/financial_ai/daily_ml/repository.py"
Task: "Ключ блокировки в backend/src/financial_ai/sync/advisory.py"
Task: "Задержка и версия в daily-ml-emulator/src/daily_ml_emulator/config.py"
```

---

## Implementation Strategy

### MVP (US1)

1. Phase 1 — артефакты и стили
2. Phase 2 — схема, эмулятор, каркас
3. Phase 3 — цикл считает сам
4. **Остановиться и проверить**: сценарии С6–С7 quickstart. Ранжирование перестало быть
   ручной командой — это уже польза, даже без интерфейса

### Инкрементальная поставка

1. Phase 1 + Phase 2 → основание
2. + US1 → цикл замкнулся (MVP)
3. + US2 → данные едут вперёд сами
4. + US3 → перезапуск ничего не повторяет
5. + US4 → цикл виден и управляем
6. + US5 → ранжирование доведено до плана
7. Phase 8 → документация, сверка с дизайном, гейт

### Где разрезать, если объём великоват

Естественный разрез — **после US3**: получается работающий, самовосстанавливающийся цикл
без интерфейса. US4 и US5 можно вынести в отдельную фичу, не переделывая ничего из
сделанного: контракты и схема к тому моменту уже зафиксированы.

---

## Notes

- `[P]` — разные файлы, нет незакрытых зависимостей
- Разметка и стили **переносятся** из артефакта Open Design; сверка по `data-od-id`
- Демонстрационные данные прототипа — цены, лоты, `DEMO_`-активы, захардкоженное число
  активов — не переносятся (`contracts/ui-states.md`, §5)
- Коммит после каждой задачи или логической группы
- На любом чекпойнте можно остановиться и проверить историю отдельно

---

## Phase 9: Convergence

- [X] T072 CRITICAL: подключить `advance()` к работающей системе — вызов при старте `backend/src/financial_ai/worker/app.py` и в тике `backend/src/financial_ai/market_data/scheduler.py` вместо прямого `pending_sessions`, чтобы торговый календарь синхронизировался и граница данных двигалась сама, per FR-006, FR-028, US2/AC1 (contradicts)
- [X] T073 CRITICAL: убрать нарезку разрыва в `backend/src/financial_ai/market_data/scheduler.py` — при разрыве сверх `MARKET_DATA_STARTUP_RECOVERY_MAX_SESSIONS` автоматический сбор не выполняется вовсе, а не идёт по три сессии за тик; добавить тест на то, что серия тиков не закрывает разрыв сверх предела, per FR-009, FR-029, US2/AC3 (contradicts)
- [X] T074 HIGH: довести перечень блокирующих групп из `readiness.evaluate` до `GET /api/daily-ml/status` и показать его в состоянии ожидания раздела «Ранжирование»; обновить `contracts/daily-ml-lifecycle-api.md` и `contracts/ui-states.md` раньше кода, per US1/AC5 (partial)
- [X] T075 MEDIUM: очистить причину отказа сборки набора от технических подробностей — `ranking/dataset.py` не вкладывает текст `OSError` в сообщение, `daily_ml/reconcile.py` не выносит его в `notes`; подробности остаются в логе, per FR-038 (contradicts)

---

## Phase 10: Convergence

- [X] T076 Записать изменившееся поведение планировщика в `AGENTS.md` (раздел «Сбор рыночных данных») и `docs/architecture.md` (§6): граница данных не двигается без синхронизации календаря — пропуски считаются по сохранённому календарю, и без обновления получается замкнутый круг; календарь спрашивается не чаще раза в сутки, отметка берётся из таблицы исходов сбора, а не из памяти процесса; разрыв сверх предела не собирается вовсе, а не нарезается по несколько сессий за тик, per plan: Constitution IX — «поведение планировщика» (partial)
