# Результаты исправления полноты и эффективности сбора

План: [remediation-plan.md](./remediation-plan.md). Задачи T203–T214.

Этот документ ведётся по ходу работы. Шаг отмечается выполненным только после
выполнения его критерия приёмки с указанием доказательства.

## Состояние задач

| Задача | Состояние | Доказательство |
|---|---|---|
| T203 Требования и исходное состояние | выполнена | раздел «T203» ниже; правки `spec.md`, `data-model.md`, контрактов |
| T204 Decimal вместо float в ISS | выполнена | раздел «T204»; 3 новые регрессии падали до правки |
| T205 Частичные глобальные ряды и ЦБ | выполнена | раздел «T205»; 8 регрессий |
| T206 Поиск истории фьючерса | выполнена | раздел «T206»; живая сверка + 4 регрессии, 2 теста дефекта переписаны |
| T207 Сохранение позиций при ошибке/остановке | не начата | |
| T208 Единое правило полноты | не начата | |
| T209 Потребители правила полноты | не начата | |
| T210 Ограничение работы выбором и окнами | не начата | |
| T211 Лишние обращения и измерение | не начата | |
| T212 Аудит и восстановление данных | не начата | |
| T213 Полная проверка | не начата | |
| T214 Документация и передача | не начата | |

## T203. Исходное состояние: снято заново 2026-09-20

Срез снят на ветке `fix/market-data-completeness`, отведённой от
`feature/collection-view-instrument-links`. Ревизия кода — `1f5664d`
(«fix: finalize market data collection feature»).

Источник среза: работающий стенд `deployments/docker-compose`, база
`financial-ai-postgres-1`, опорная дата `asof_date = 2026-09-18`.
Данные инвестиционного счёта и секреты в срез не входят: измерены только
рыночные ряды и журнал прогонов.

### Объём хранилища

| Таблица | Строк |
|---|---|
| `market_trading_session` | 3409 (последняя сессия 2026-09-18) |
| `market_equity_daily_bar` | 97 877 |
| `market_global_daily_series` | 14 290 |
| `market_futures_position` | 8 816 |
| `market_ingest_run` | 3 411 |

### Расхождение сводки с хранилищем

Сводка `GET /api/market-data/coverage` на той же базе объявляет группу
«глобальные ряды» полной: `coverage_ratio = 1.0`, `gaps = 0`, и у источника
`brent` — `sessions_covered = 314`, `failures_total = 0`.

Прямой счёт по той же базе и тому же окну 314 сессий:

| Проверка | Результат |
|---|---|
| Сессий в окне | 314 |
| Дат Brent с непустым значением | 192 |
| Дат Brent без значения | 122 |
| Brent за 2026-03-16 | в БД отсутствует |

То есть при 122 отсутствующих датах сводка показывает полное покрытие.
Причина видна в журнале: `brent` имеет **219 исходов `ok` с `rows_written = 0`**,
и действующее правило полноты закрывает такой сессией источник.

### Подозрительные исходы журнала

Исходы `ok` с нулём записанных строк по всей истории:

| Источник | Пустых `ok` |
|---|---|
| `brent` | 219 |
| `trading_calendar` | 57 |
| `futures_positions` | 10 |
| `index_constituents` | 2 |
| `equity_sectors` | 2 |
| `equity_agg` | 2 |

Распределение исходов целиком (`status` × источник) снято и сохранено:
`futures_positions` — 402 `ok` / 30 `failed` / 23 `stopped`;
`cbr` — 203 `ok` / 57 `failed`; `equity_d1` — 576 `ok` / 2 `failed` / 2 `skipped`.

Доказанный случай: позиции за 2026-09-04 — исход `ok`, `rows_written = 0`,
в `market_futures_position` за эту дату **0 строк**.

### Обязательные точки кривой ЦБ

Из четырёх точек, требуемых моделью (`REQUIRED_ZCYC_TERMS` = `yield_1y`,
`yield_2y`, `yield_5y`, `yield_10y`), в окне после 2026-08-15 отсутствуют **все
четыре** на датах: 2026-08-24, 2026-08-25, 2026-08-26, 2026-08-27, 2026-08-28,
2026-09-16. Ряды `CBR_ZCYC_*` имеют по 308 значений против 311 у
`CBR_KEY_RATE` — ставка сохранялась там, где кривая не сохранилась.

### Готовность модели против фактических данных

На той же базе `GET /api/daily-ml/status` отвечает:
`latest_data_ready = 2026-09-18`, `data_gap_sessions = 0`,
`blocking_groups = []`, `readiness_known = true`.

Готовность объявлена при 122 отсутствующих датах Brent в том же окне.

### Потеря точности при разборе JSON

Воспроизведено вне проекта на том же приёме разбора, что применяется к ответам
ISS: `123456789.123456789` через промежуточный `float` даёт
`123456789.12345679`; при разборе сразу в `Decimal` — исходное число.

### Исторические отметки

Прежний полный `scripts/check.sh` на `1f5664d` прошёл: 809 backend, 61 emulator,
231 frontend тест. Эта отметка сохраняется как история и **не** является
подтверждением устранения перечисленных выше дефектов: ни один из них тем
прогоном не обнаружен.


## T204. Разбор ISS: float убран с пути значения

**Причина.** `_get_json()` возвращал `response.json()`, а он разбирает дробные числа JSON
в `float`. Перевод в `Decimal` у потребителя потерю не возвращает: к этому моменту
восстанавливать уже нечего.

**Правка.** Одна точка перевода — `iss/client.py:_loads()`:
`json.loads(body, parse_float=Decimal)`. `to_decimal()` пропускает `Decimal` как есть и
предупреждает, если значение всё-таки пришло как `float`, — тогда потеря произошла до
разбора, и место видно по логу.

**Почему дефект не ловился.** Все тесты `test_precision.py` подавали значения **строками**,
а на строках потери нет никогда. HTTP-путь не проходил ни один из них: ISS отдаёт числа
JSON без кавычек.

**Доказательство.** Четыре новые регрессии в `backend/tests/market_data/test_precision.py`
проходят ответ биржи целиком — байты HTTP → разбор → домен:

| Проверка | До правки | После |
|---|---|---|
| `123456789.123456789` в ответе | `123456789.12345679` | `123456789.123456789` |
| тип дробного значения | `float` | `Decimal` |
| `1e-9` | `1.0000000000000001e-09` (float) | `Decimal("1E-9")` |
| `null` | — | остаётся `None`, нулём не становится |
| `0` | — | `0`, отличается от `None` |

Три из четырёх падают на прежнем коде (проверено подменой `_loads` обратно на
`response.json()`), четвёртая — сторожевая на `null`/`0`.

Потребители ответа проверены: котировки, агрегаты, глобальные ряды, Brent, справочники,
открытый интерес и размер лота. Целые числа `json` разбирает в `int` и без правки,
`int(Decimal)` в открытом интересе и лоте работает без изменений.

Прогон `tests/market_data/` целиком на живой PostgreSQL: **409 passed**.


## T205. Частичный сбор перестал быть успехом

**Причина.** `_fetch_series()` возвращал `{}` и при ошибке транспорта, и при корректном
пустом ответе. Обход рядов шёл дальше, итог складывался из уцелевших, и источник
записывался `ok`. Один полученный ряд из пяти закрывал сессию всем пяти.

У ЦБ причина зеркальная: ошибка кривой поднималась выше и уносила весь источник вместе с
уже полученной ставкой — отсюда 311 значений `CBR_KEY_RATE` против 308 у каждой точки
`CBR_ZCYC_*` на стенде.

**Правка.**

- `SeriesFetchError` отделяет несостоявшееся обращение от корректного пустого ответа.
  Пустой словарь теперь означает ровно одно: биржа ответила, значений нет.
- `SourcePartialError` (в `interrupt.py`, рядом с `SourceStoppedError`) — исход
  «полученное записано, применимая работа сделана не вся». Несёт фактическое
  `rows_written` и перечень незавершённого словами.
- `run_source()` записывает его как `failed` с фактическим числом строк: полученное
  остаётся в хранилище, но сессию по источнику не закрывает.
- `_sync_cbr_range()` собирает ставку и кривую независимо: падение любой из них не
  отменяет другую, полученное сохраняется, незавершённое называется в исходе.
- Счётчик подряд идущих неудач видит эти исходы без правок:
  `health.record(source_id, outcome.status != STATUS_FAILED)`.

**Доказательство.** `backend/tests/market_data/test_partial_sources.py`, 8 проверок —
ровно сценарии приёмки T205:

| Сценарий | Ожидание | Результат |
|---|---|---|
| 1 успешный ряд + 4 ошибки | ряд сохранён, источник незавершён | `rows_written=1`, `unfinished=(RTSI, RGBI, RVI, USD_ISS)` |
| все ряды упали | неуспех, сохранять нечего | `rows_written=0`, записи не было |
| все ряды собраны | источник закрыт | исключения нет, `written=5` |
| корректный пустой ответ | не неуспех | `written=0`, записи не было |
| остановка посреди обхода | отдельный исход | `SourceStoppedError(rows_written=1)` |
| ставка + ошибка кривой | ставка сохранена, кривая — работа | `rows_written=1`, кривая в `unfinished` |
| ошибка ставки + кривая | кривая сохранена | `rows_written=1`, ставка в `unfinished` |
| обе части собраны | источник закрыт | `written=2` |

Прогон `tests/market_data/` целиком: **417 passed** (было 409 до добавления этих восьми).


## T206. Сбой, отсутствие и уже полученный ответ — три разные вещи

**Причина.** `_has_data()` ловил `PositionsSourceError` и возвращал `False`. Упавшая проба
читалась как доказанное отсутствие данных: все обращения могли упасть, а источник
заканчивался исходом `ok` с нулём строк. Отрицательный результат при этом запоминался, и
один сбой сети закрывал контракт на весь прогон.

Вторая причина — сама сетка проб. Шаг задаётся настройкой (по умолчанию 50 сессий), а у
позиций законно бывают пустые даты: контракт существует, сделок в этот день нет. Одна
отрицательная проба в такой день объявляла несуществующим инструмент, торгуемый годами, и
правило `return not searched_for_first_date(contract)` закрывало его навсегда.

**Живая сверка перед правкой** (2026-09-20, `www.moex.com`, без записи в БД):

| Проверка | Результат |
|---|---|
| Список инструментов на 2025-07-01 / 2026-09-04 / 2026-09-17 | одинаковый, 263 контракта, 1 обращение на дату |
| `SBRF_F` за 2026-09-04 | `fiz_long=140889`, `fiz_short=80491`, `jur_long=385395`, `jur_short=445793` |
| `SBRF_F` за 2026-09-03 | `fiz_long=142626`, `fiz_short=78953`, `jur_long=380039`, `jur_short=443712` |

Первое измерено, а не предположено, и определило решение: **список инструментов от даты не
зависит**, поэтому признаком применимости к дате он быть не может. Второе подтверждает
доказанный пропуск: за 2026-09-04 данные у источника есть, в БД — ноль строк при исходе `ok`.

**Правка.**

- `_has_data()` ошибку не ловит: незавершённая проба оставляет границу неизвестной.
- `first_available_date()` — нижняя граница существования, а не дата появления контракта.
  `None` означает «не найдено» и доказательством отсутствия истории не является;
  отрицательный ответ после сбоя не запоминается.
- Проверка «существовал ли инструмент тогда» убрана из пути сбора: доказательства
  отсутствия этот источник не даёт, а её пробы сокращать работу больше не могли. Нужные
  даты проверяются обращением. Поиск остался у клиента для аудита T212.
- Кеш снимков прогона (`SNAPSHOT_CACHE_LIMIT = 512`): найденный при поиске снимок для той
  же пары «контракт — дата» вторично не запрашивается. Живёт ровно столько, сколько
  клиент, то есть один прогон.
- Остановка внутри повторов `_post()` — `SourceStoppedError`, а не ошибка источника.

**Доказательство.** Четыре новые регрессии в `test_positions_source.py`:
сбой пробы передаётся наружу и не запоминается как «нет»; после сбоя поиск идёт в источник
снова; найденный снимок не запрашивается второй раз; остановка отменяет повторы своим
исходом (1 попытка вместо 4).

**Два существующих теста переписаны, и это не ослабление проверок.** Они закрепляли само
правило потери данных:

| Было | Стало |
|---|---|
| `test_period_before_the_instrument_existed_is_not_asked`: сессия раньше найденной границы не спрашивается, `calls == []` | `test_date_before_the_found_lower_bound_is_still_asked`: дата спрашивается и **приносит значения** (`written == 1`) |
| `test_contract_without_any_data_is_not_asked_again`: «искали и не нашли — обращения бесполезны», `calls == []` | `test_contract_without_probe_hits_is_still_asked`: пара спрашивается, ноль значений на выполненных обращениях даёт `EmptyPositionsError` |

Два теста темпа обращений (`test_pause_is_kept_between_batches`,
`test_batch_size_is_configurable`) теперь спрашивают РАЗНЫЕ даты: пять обращений за одну
пару после появления кеша проверяли бы уже не темп, а кеш.

Прогон `tests/market_data/` целиком: **421 passed**.

## Продолжение работ после T206 (2026-09-20)

Рабочие изменения по T207–T211 внесены, но приёмка ещё не пройдена и задачи не закрываются:

- T207: позиции сохраняются пачками с commit; при поздней HTTP-ошибке исход несёт фактический `rows_written` и незавершённую пару; повтор проверяет пару `asset_id + contract_code + session_date`. Добавлены регрессии в `test_positions_partial_save.py`.
- T208–T209: добавлены `coverage_version`, `coverage_reason`, сохранённая граница перехода, `requires_audit`; проверка полноты переключена с факта наличия строки на успешный исход текущей версии. Обновлены readiness/ingest, gaps, coverage API и UI. Нужны исправление регрессий, полноценное T212 и повторные проверки.
- T210: планирование catchup учитывает выбранные группы и их отдельные окна. Есть регрессия выбранной группы; общий гейт выявил старые тестовые сценарии, ожидающие закрытие по наблюдениям.
- T211: ежедневное обновление календаря теперь берёт ограниченное перекрытие от последней сохранённой сессии; после пустого календаря и явного диапазона сохраняется полная загрузка. Добавлены тесты. Оптимизация Brent и сопоставимые метрики SQL/HTTP не выполнены.

### Первая проверка полного гейта

Запущено `scripts/check.sh` (полный режим, включая Docker). Alembic на пустой БД, daily-ml-emulator checks, frontend eslint/vitest, Docker builds прошли. Гейт завершился с провалом:

- backend ruff: ошибки форматирования/импортов (после запуска исправлены автоматически; гейт повторно не запускался);
- backend pytest: **23 failed, 814 passed**. Часть старых тестов проверяет прежнюю семантику «наблюдение само закрывает сессию»; их нужно обновить согласно T208. Остальные расхождения планирования/покрытия нужно разобрать пофайлово;
- frontend prettier: исправлено;
- frontend tsc: отсутствовавшее поле `requires_audit` в фикстуре исправлено;
- frontend vitest и Docker build прошли.

Невыполнено: T212 (команда dry-run/repair с сохраняемым бюджетом HTTP, продолжением и адресным исправлением), полный зелёный повтор `scripts/check.sh`, живые сверки, OpenDesign-синхронизация (MCP OpenDesign вернул `Transport closed`), актуализация итоговых документов и критерии завершения. Модуль завершённым не объявлять.

### T207: приёмочные сценарии после запуска PostgreSQL

Локальный `postgres` поднят через Compose; с `DATABASE_URL` на порт `5433` пройдены
`tests/market_data/test_positions_partial_save.py` и
`tests/market_data/test_positions_cleanup.py`: **16 passed**. Проверены сохранение до
поздней ошибки и остановки, commit переживает rollback как модель холодного рестарта,
повтор запрашивает только незавершённую пару, смена семейства не скрывает работу, а
пустое значение повторного ответа не затирает сохранённое. Это доказательство T207;
полный гейт остаётся общим критерием завершения модуля.

### T208: доказуемое правило полноты

В `IngestRun` добавлены `coverage_version` и `coverage_reason`; миграция `0014`
однократно сохраняет границу старой области, не присваивая старым исходам новую версию.
Сессия закрывается только успешным исходом текущей версии, а старые неподтверждённые
сессии помечаются `requires_audit` и не становятся автоматическим историческим догоном.
Поздний `failed`/`stopped` перевешивает частично сохранённые строки; успешное продолжение
снова закрывает свою дату.

С PostgreSQL пройдены `tests/unit/test_completeness_rule.py`,
`tests/market_data/test_coverage.py` и `tests/market_data/test_gaps.py`: **35 passed**.

### T209: единое правило у потребителей

Сводка, поиск пропусков, построение набора и запуск ранжирования используют
подтверждённые исходы и сохраняют источник причины по каждой сессии. Поздний отказ
диапазонного источника виден в сводке, а неполнота обязательной группы останавливает
запрос к ранжированию. UI показывает требующий аудита старый диапазон.

После обновления фикстур под подтверждённые исходы пройдены затронутые backend-сценарии:
**116 passed**, затем ещё **2 passed** для диапазонного окна и manifest набора. Полный
гейт будет повторён после T210–T212.
