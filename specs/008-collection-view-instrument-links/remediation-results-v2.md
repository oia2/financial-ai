# Доказательства исправлений T215–T233

План — [remediation-plan-v2.md](./remediation-plan-v2.md).
Основание — [ревью 2026-09-21](./review-2026-09-21.md).
Отметки выполнения ведутся только в [tasks.md](./tasks.md); этот файл хранит доказательства.

## Исходное состояние

На момент подготовки плана реализация T215–T233 не начата. Код — `f4042c8`.
Состояние БД, результаты живых сверок и полный гейт предыдущего ревью описаны в отчёте.
Они являются исходным срезом, а не доказательством выполнения новых задач.

Новый ремонт рабочей БД этим документом не разрешён и не запускался. Перед T231
записываются конкретный план, его область, бюджет и основание разрешения из сессии.

## Запись завершённого шага

Для каждой задачи добавить отдельную запись по образцу, заполнив фактами:

```text
Задача / дата:
Ревизия или точный рабочий diff:
Какой сценарий был неверным:
Что изменено / файлы:
Регрессия и наблюдаемый результат до/после:
Общий гейт: команда, exit code, прошедшие/пропущенные проверки:
Живая сверка / сравнение БД, если требуется:
HTTP / SQL / время, если требуется:
Ограничения и невыполненные пункты приёмки:
Ссылка на факт, позволяющий поставить отметку в tasks.md:
```

Не копировать числа прошлой задачи как результат нового запуска. Не сохранять секреты,
полные конфигурации окружения и данные инвестиционного счёта.

## Передача следующей сессии

- Следующая задача: **T219**.
- Выполненные задачи этой серии: **T215–T218**.
- Незавершённые изменения реализации: версия 2 и хранилище готовы; источники начнут
  формировать доказательства и закрывать покрытие в T219.
- Сначала прочитать: AGENTS.md, Constitution, разделы 1–3 плана и T215.
- Граница следующего шага: валидация контрактов ответов ISS; хранение доказательств и
  миграция остаются T218.
- Разрешённый объём нового ремонта: не задан; требуется конкретный план T230.
- Внешние ограничения из ревью: браузер был недоступен; в будущей сессии проверить заново.

После каждого шага обновлять эту передачу: текущий/следующий T, фактическую ревизию,
незавершённый diff, требуемые повторные проверки и точные ограничения. Исторические
доказательства завершённых шагов выше не удалять.

## T215 · Целевые правила и переход версии · 2026-09-21

**Ревизия или точный рабочий diff:** документационный diff относительно `e942f2b` в
`spec.md`, `plan.md`, `data-model.md` и трёх файлах `contracts/`.

**Какой сценарий был неверным:** версия 1 могла присваиваться `ok` после ответа с неверным
контрактом; число новых строк смешивалось с доказательством полноты, итог выбранного плана
— с полнотой всей группы, а непроверенный справочник — с ошибкой транспорта.

**Что изменено / файлы:** FR-032a/c и новые FR-033a/b задают шесть исходов единицы работы,
версию 2, отдельный итог выбранного плана и состояние справочника `requires_audit`.
`data-model.md` определяет `market_source_work_evidence` и неизменяемую границу версии 2.
Контракты сводки, состояния прогона и UI согласованы с этими правилами. `plan.md` явно
отделяет целевой контракт от ещё не выполненной реализации.

**Регрессия и наблюдаемый результат до/после:** код в T215 намеренно не менялся; ревью R2
является воспроизведением ложного успеха до изменения. После изменения проектные артефакты
однозначно запрещают назначать версию по `status`/`rows_written` и запрещают доверять
версии 1 без повторной проверки.

**Общий гейт:** `scripts/check.sh --no-docker`, exit code 0 после запуска штатной тестовой
PostgreSQL: backend 838 passed; Alembic 0001–0015 на чистой БД; emulator 61 passed;
frontend 231 passed; lint, форматирование, mypy, tsc и сборка статики прошли. Первый запуск
до старта PostgreSQL завершился на шаге Alembic с отказом подключения и доказательством не
считался. Docker-сборка этим быстрым гейтом не выполнялась и остаётся общей приёмкой T232.

**Живая сверка / сравнение БД:** не требуется для документационной задачи; сеть и рабочая
БД не изменялись.

**Ограничения и невыполненные пункты приёмки:** версия 2, таблица доказательств и UI ещё не
реализованы; это явно оставлено T216–T226. Новый ремонт не разрешён и не запускался.

**Ссылка на факт, позволяющий поставить отметку:** согласованные FR-032a/c, FR-033a/b,
`market_source_work_evidence`, три API/UI-контракта и успешный единый быстрый гейт выше.

## T216 · Валидация формы ответов ISS · 2026-09-21

**Ревизия или точный рабочий diff:** код и регрессии относительно `267d3b6` в
`market_data/iss/client.py`, `test_iss_client.py` и уточнённой фикстуре
`test_securities.py`; правило пакетного запуска гейта записано в `remediation-plan-v2.md`.

**Какой сценарий был неверным:** HTTP 200 с отсутствующим `history` превращался в пустой
успех; недостающие колонки, короткие строки и ответ за чужую дату могли пройти разбор и
получить смысл законной пустоты либо наблюдения запрошенного дня.

**Что изменено / файлы:** общий валидатор проверяет JSON-объект, ожидаемый блок,
`columns/data`, обязательные колонки потребителя, форму каждой строки и даты диапазона.
Контракты заданы для `history`, `analytics`, `series`, `securities`, `description` и
`indices`. Преобразование строк использует строгий `zip`; Decimal разбирается в `_loads`
до валидации, законные `NULL` сохраняются.

**Регрессия и наблюдаемый результат до/после:** добавлены сценарии неожиданного блока,
корректного пустого блока, отсутствующей ценовой колонки, `NULL` в существующей колонке,
короткой строки, чужой даты и обязательных колонок всех справочных блоков. В полном
диагностическом прогоне эти регрессии и существующие проверки точности прошли; два старых
DB-теста упали только из-за неполной фикстуры `securities` без ISIN. После приведения
фикстуры к фактическому общему ответу оба сценария дали `2 passed`.

**Общий гейт:** диагностический `scripts/check.sh --no-docker` дошёл до backend и дал
`848 passed, 2 failed`; Alembic, emulator 61 и frontend 231 прошли. Повтор общего гейта был
остановлен по указанию владельца как избыточный после прохождения lint/format/mypy и трети
backend-набора. Точечно подтверждены lint/format трёх изменённых файлов и два исправленных
DB-сценария (`2 passed`). Следующий пакетный общий гейт будет выполнен после существенного
блока связанных кодовых задач согласно обновлённому плану.

**Живая сверка / сравнение БД:** рабочая БД не изменялась; живое сравнение размеров страниц
относится к T217.

**Ограничения и невыполненные пункты приёмки:** логика завершения пагинации ещё опирается
на запрошенный limit и исправляется в T217. Присвоение `coverage_version` остаётся T219.

**Ссылка на факт, позволяющий поставить отметку:** явные `IssError` до преобразования и
регрессии всех перечисленных форм ответа; точечные проверки после единственных падений
прошли.

## T217 · Пагинация по фактическому ответу сервера · 2026-09-21

**Ревизия или точный рабочий diff:** diff относительно `67ca7e0` в ISS client/urls и
трёх файлах тестов клиента.

**Какой сценарий был неверным:** при `limit=1000` сервер возвращал максимум 100 строк,
клиент принимал короткую страницу за последнюю и молча терял 406 строк TQBR.

**Что изменено / файлы:** запросы истории включают `history.cursor`. Для `history` и
`analytics` проверяются `INDEX/TOTAL/PAGESIZE`, следующий `start` увеличивается на
фактическое число строк, конец определяется `TOTAL`. При отсутствии курсора обход идёт до
явно пустой страницы; повтор страницы, отсутствие продвижения, противоречивый курсор и
пустая страница до `TOTAL` завершаются `IssError`. Остановка и HTTP-бюджет проверяются
перед каждым следующим физическим запросом.

**Регрессия и наблюдаемый результат до/после:** 63 целевых теста прошли. Добавлены cap
1000→2, несколько страниц, пустой диапазон, последняя страница ровно полного размера,
fallback без курсора, повтор страницы, поздний HTTP-отказ, остановка и исчерпание бюджета
между страницами, а также отдельный курсор analytics. Тесты URL подтверждают запрос
курсора; Decimal-регрессии используют валидный курсор и прошли.

**Общий гейт:** по правилу пакетной проверки после T216 не повторялся. Целевые lint и
format пяти изменённых файлов прошли; целевые тесты — `63 passed`. Ближайший общий гейт
будет выполнен после следующего существенного блока связанных задач.

**Живая сверка / HTTP:** TQBR 2026-09-18, колонки SECID/TRADEDATE/OHLCV: limit 100 и
limit 1000 дали по 506 строк, по 6 HTTP-попыток, первый SECID `ABIO`, последний `ZVEZ`;
полное содержимое совпало, SHA-256
`0ce30865e810daa837b828d11d251ee8a20523c18814f83311ad2bdb9a8220a6`.
Analytics IMOEX за ту же дату: оба limit дали 44 строки за одну попытку, одинаковый
SHA-256 `d5cc049d713bf24635a6b97e04f3cfd50ab88122bce5d53358cdb49e739f5da4`.

**Ограничения и невыполненные пункты приёмки:** живой источник ограничивает страницу
истории 100 строками независимо от запрошенного 1000; новая логика это поддерживает и
потому не запрещает 1000 конфигурацией. Покрытие результата внедряется в T218–T219.

**Ссылка на факт, позволяющий поставить отметку:** равенство полных живых результатов по
содержимому, целевые 63 теста и явная проверка курсоров обоих постраничных блоков.

## T218 · Доказательства единиц работы и миграция версии 2 · 2026-09-21

**Ревизия или точный рабочий diff:** diff относительно `5539b61`: модель и репозиторий
рыночных данных, `coverage_rule.py`, миграция 0016, тесты хранения, общая очистка тестовой
БД и `data-model.md`.

**Какой сценарий был неверным:** `market_ingest_run` хранил только общий `ok` и число
строк; доказать результат конкретного ряда/пары после рестарта было невозможно, а версия 1
уже содержала ложные успехи.

**Что изменено / файлы:** добавлена `market_source_work_evidence` с ключом
`source_id/session_date/work_key/coverage_version`, видом результата, кодом основания,
временем и исходным run id. Повтор не переписывает происхождение доказательства. Текущая
версия правила — 2. `record_run()` больше не выводит версию из `status=ok` или числа строк:
версия и причина принимаются только вместе от явной проверки. Миграция 0016 создаёт
таблицу и однократную границу версии 2, сохраняя версию 1 и старые данные.

**Регрессия и наблюдаемый результат до/после:** 6 PostgreSQL-тестов прошли: уникальность и
первое происхождение, чтение после нового соединения, разные семейства одной бумаги,
различие нулевого и NULL-наблюдения с основаниями, отсутствие автоматического
доказательства у `ok`, сохранение обеих границ и пустая база без границы.

**Общий гейт:** отложен до завершения T219 как один связанный переход хранения и
потребителей. Целевые ruff/format прошли; целевые PostgreSQL-тесты — `6 passed`.

**Проверка миграции:** на временной существующей БД выполнены upgrade до 0013, вставка
сессий 2026-09-17/18, upgrade 0014–0016. Получены границы `1:2026-09-18` и
`2:2026-09-18`, доказательств 0. После вставки 2026-09-21 и повторного `upgrade head`
обе границы остались 2026-09-18. На пустой временной БД полный upgrade дал 0 границ и 0
доказательств. Обе временные БД удалены.

**Ограничения и невыполненные пункты приёмки:** после безопасного отключения автоматической
версии существующие источники пока не закрывают версию 2; их явные результаты и все
потребители переводятся в T219, поэтому общий гейт сознательно сгруппирован с ним.

**Ссылка на факт, позволяющий поставить отметку:** сохраняемая после нового соединения
пара `EQ_AST_SBER/SBRF_F`, неперезаписываемое происхождение, две отдельные семьи контракта
и результаты обеих миграционных БД выше.

## T219 · Покрытие только после явной проверки · 2026-09-22

**Ревизия или точный рабочий diff:** diff относительно `ae48c71` в оркестраторе сбора,
правиле полноты, источниках дневных данных и регрессиях backend.

**Какой сценарий был неверным:** `run_source()` превращал любой возвращённый `int` в
`status=ok`; число записанных строк не доказывало, что источник проверил всю применимую
работу. После T218 потребители по-прежнему смотрели только на общий исход прогона и не
требовали сохранённых доказательств.

**Что изменено / файлы:** введён `VerificationResult` с явными доказательствами
`session_date/work_key/result_kind/reason_code`. Дневные доски, пять глобальных рядов,
Brent, состав IMOEX, две независимые семьи ЦБ и позиции возвращают проверенный результат.
Диапазон получает точный список торговых сессий: частичный ответ доказывает только
присутствующие даты, полностью пустой корректный ответ фиксирует подтверждённое отсутствие.
`run_source()` сохраняет доказательства и ставит coverage v2 только полному результату;
старый числовой контракт для сессионного источника завершается неуспехом. Календарь и
суточные справочники остаются вне сессионного покрытия. `completeness.closed_sessions()`
теперь пересекает текущий успешный исход с полным набором обязательных work keys; сводка,
календарь, догон, readiness и dataset используют этот единый расчёт.

**Регрессия и наблюдаемый результат до/после:** проверены один успешный ряд из пяти,
пять ошибок, пять успехов, корректный пустой ответ, ставка ЦБ при падении кривой, кривая
при падении ставки, отсутствующая дата внутри диапазона, поздний failed, успешное
продолжение и запрет выдавать `int` за доказательство. Тестовые seed-сценарии переведены
на тот же явный v2-контракт, чтобы тесты не обходили производственное правило.

**Общий гейт:** пакетный `scripts/check.sh --no-docker` подтвердил ruff check, mypy,
Alembic 0001–0016, emulator 61, frontend 231 и сборку статики; format указал два
неформатированных файла, backend pytest выявил 80 старых seed-сценариев без v2-доказательств
(`786 passed, 80 failed`). После их перевода и исправления суточного гейта повторены только
два проваленных шага по указанию владельца: format — `204 files already formatted`, полный
backend pytest — `866 passed`.

**Живая сверка / сравнение БД:** рабочая БД не изменялась. Хранение и миграция проверены в
T218, живые ISS-контракты и полнота страниц — в T216–T217.

**Ограничения и невыполненные пункты приёмки:** семантика неизвестного ответа и продолжения
по отдельным парам позиций остаётся T220–T221; единый итог выбранной работы и рестарт —
T222. Полный гейт с Docker выполняется один раз в T232.

**Ссылка на факт, позволяющий поставить отметку:** `866 passed`, явный провал legacy-`int`
без `coverage_version`, доказательства всех обязательных work keys и одинаковый расчёт
неполноты во всех потребителях.

## T220 · Различимые результаты клиента позиций · 2026-09-22

**Ревизия или точный рабочий diff:** diff относительно `1dec616` в
`sources/positions_client.py`, переходной адаптации `sources/positions.py` и
`test_positions_source.py`.

**Какой сценарий был неверным:** `fetch()` возвращал один `None` для отсутствующей таблицы,
снимка за чужую дату и отсутствующего кода в текущем списке инструментов. Неизвестность
становилась неотличима от законного отсутствия данных.

**Что изменено / файлы:** клиент возвращает `PositionFetchResult` с видом `value`,
`confirmed_absence` или `unknown` и стабильным кодом причины. Отсутствующая таблица,
чужая дата и отсутствие кода в текущем списке дают `unknown`. Подтверждённым отсутствием
считается только валидная строка позиций за точную дату с явно пустыми четырьмя полями.
Числовые нули остаются значениями. Различимый результат пары кешируется на время прогона;
состояние VIEWSTATE остаётся последовательным. Транспортные ошибки и остановка по-прежнему
передаются исключениями и не превращаются ни в отсутствие, ни в неизвестный ответ.

**Регрессия и наблюдаемый результат до/после:** проверены точная таблица, отсутствующая
таблица с повтором из кеша, чужая дата, отсутствующий код без HTTP, нулевые позиции,
частичные поля, ограниченные 429/сетевые повторы, сбой поиска, остановка и повтор уже
полученного снимка. Целевые тесты клиента и источника — `44 passed`; ruff, format и mypy
изменённых файлов прошли.

**Общий гейт:** не повторялся после успешного пакетного гейта T219; по принятому правилу
следующий пакетный запуск выполняется после существенного связанного блока.

**Живая сверка / сравнение БД:** новых обращений к живому источнику не выполнялось; формы
основаны на уже сохранённых живых примерах SBRF_F. БД не изменялась.

**Ограничения и невыполненные пункты приёмки:** продолжение после неизвестной пары,
сохранение оснований по применимым парам и классификация отказов источника выполняются в
T221. Переходная поддержка старых тестовых клиентов `snapshot/None` остаётся до перевода
оркестрации пар.

**Ссылка на факт, позволяющий поставить отметку:** три значения `PositionFetchKind`,
отдельные reason codes для трёх неизвестных форм и `44 passed`.

## T221 — продолжение позиций по доказанным парам

**Статус:** выполнено.

**Ревизия или точный рабочий diff:** изменения после `373c741` в `sources/positions.py`,
`ingest.py`, `interrupt.py` и целевых тестах позиций, догона и правил ревью.

**Какой сценарий был неверным:** продолжение считало пару готовой по любой непустой строке
позиций. Доказательство успешной пары или подтверждённого отсутствия не сохранялось при
частичной ошибке и мягкой остановке. Любой неуспешный исход увеличивал счётчик
недоступности, поэтому несколько неизвестных пар могли закрыть источник для последующих
доступных дат.

**Что изменено / файлы:** единицей продолжения стал ключ
`pair:<asset_id>:<contract_code>` в доказательствах coverage v2. Наблюдение без такого
доказательства, включая legacy-строку и прежнее семейство контракта, пару не закрывает.
Явное значение и подтверждённое отсутствие сохраняют доказательство; `unknown` оставляет
конкретную пару незавершённой и не считается недоступностью источника. Частичная ошибка и
остановка переносят уже полученные доказательства в исход, а `run_source` сохраняет их с
идентификатором прогона. Счётчик отказов теперь изменяется по
`counts_as_unavailable`, а не по любому статусу `failed`. `_sync_positions` передаёт полный
`VerificationResult` без повторного сведения результата к числу строк.

**Регрессия и наблюдаемый результат до/после:** проверены один успех плюс неизвестная
пара, подтверждённое отсутствие и повтор с нулём HTTP, продолжение после частичной ошибки
и остановки, старая строка без evidence v2, смена семейства, сохранённая пачка и серия
недоступности в догоне. Целевой набор позиций/догона/правил — `134 passed`; дополнительная
регрессия подтверждённого отсутствия — `1 passed`. Ruff, format и mypy изменённых модулей
прошли.

**Общий гейт:** не запускался: T221 проверен целевым набором, полный дорогой гейт оставлен
на T232 либо следующий существенный пакет кода. После пакетного гейта повторяются только
упавшие проверки, если изменения не затрагивают остальные.

**Живая сверка / сравнение БД:** внешних обращений не выполнялось. В тестовой PostgreSQL
проверено сохранение pair-evidence вместе с частичным исходом и повтор без обращения для
значения и подтверждённого отсутствия.

**Ограничения и невыполненные пункты приёмки:** единый итог всей выбранной работы и
сохранение состава плана после рестарта относятся к T222; бюджет и холодный рестарт
ремонта — к T225–T226.

**Ссылка на факт, позволяющий поставить отметку:** продолжение читает только
`market_source_work_evidence` текущей версии, исключения несут сохранённые доказательства,
а неизвестный результат возвращает `complete=False, counts_as_unavailable=False`.

## T222 — единый итог выбранной работы

**Статус:** выполнено.

**Ревизия или точный рабочий diff:** изменения после `00e238b` в `plan.py`, `ingest.py`,
`runner.py`, `scheduler.py`, `journal.py`, модели/репозитории, миграции `0017` и целевых
тестах.

**Какой сценарий был неверным:** выполнение объявляло сессию закрытой по одному исходу
`equity_d1`, даже если позиции, состав индекса или диапазонный источник не отдали данные.
Журнал после рестарта считал всякую дату без failed-строки собранной и видел диапазонный
исход только на дате конца периода. Точный состав выбранной работы не сохранялся, поэтому
восстановить прежний итог без сегодняшнего плана было невозможно.

**Что изменено / файлы:** `plan.fold_session_outcome` сворачивает статусы всех выбранных
источников во взаимоисключающие `collected`, `partial`, `failed`, `skipped` и `pending`,
с отдельным признаком остановки. Диапазонные результаты включаются в каждую дату окна,
ранее закрытый источник учитывается как `ok`, а частичная запись не закрывает сессию.
Точная свёртка, список выбранных источников и признак остановки сохраняются в новой таблице
`market_ingest_session_outcome`; миграция — `0017_ingest_session_outcome.py`. Runner и
scheduler принимают готовый итог без собственной эвристики. Журнал читает сохранённую
свёртку; старые записи без снимка плана получают `history_limited=true` и `pending`, а не
выдуманный успех или провал.

**Регрессия и наблюдаемый результат до/после:** проверены только позиции с отказом,
успешные котировки плюс упавшие позиции, отказ диапазонного источника, повтор закрытой
работы, остановка/продолжение и чтение через новое соединение. Основной целевой набор
catchup/runner/scheduler/resume дал `77 passed`; расширенный набор с контрактом и правилами
ревью прошёл 141 сценарий, единственное изменившееся ожидание callback остановки проверено
отдельно вместе с focused-набором — `34 passed`. Ruff, format и mypy прошли. Миграции
`0016 -> 0017` применились, текущая ревизия — `0017 (head)`.

**Общий гейт:** не запускался; после изменения итогов выполнены целевые backend-наборы.
Полный гейт остаётся на T232 или существенный пакет, а после его ошибки повторяются только
упавшие проверки.

**Живая сверка / сравнение БД:** внешних обращений не выполнялось. В PostgreSQL создана
таблица сохранённых итогов; тест с новым SQLAlchemy-соединением получил те же
`requested=1, partial=1, collected=0, failed=0` и `history_limited=false`.

**Ограничения и невыполненные пункты приёмки:** отображение новых полей и обновление всех
виджетов выполняются в T223. Единственный владелец запуска и ремонт после рестарта относятся
к T224–T226.

**Ссылка на факт, позволяющий поставить отметку:** исполнитель и журнал читают одну
сохранённую свёртку `market_ingest_session_outcome`; сумма пяти исходов строится по точному
числу строк плана, а старые записи явно помечены ограниченными.

## T223 — честные итоги и непроверенные справочники в UI

**Статус:** выполнено.

**Ревизия или точный рабочий diff:** изменения после T222 в `coverage.py`, `ingest.py`,
`repository.py`, DTO и виджетах страницы сбора, контрактах API и регрессионных тестах.

**Какой сценарий был неверным:** журнал не показывал `partial`, `pending` и ограниченность
старой истории, поэтому частичный прогон выглядел лучше факта. Старый успешный исход
справочника без evidence v2 мог выглядеть как подтверждённый текущий результат. После
завершения прогона обновлялась только сводка покрытия, а журнал и календарь могли оставаться
устаревшими.

**Что изменено / файлы:** сводка справочников теперь читает последний завершённый исход
источника и различает старый успех без доказательства (`Нужна проверка`), новый успех с
coverage v2 и реальный отказ с причиной и временем. Успешное чтение отраслей и лотов
записывает evidence v2. Журнал показывает `partial`, `pending` и `history_limited`.
`MarketDataPage` после перехода активного прогона в завершённый инвалидирует сводку,
журнал и календарь. Контракты обновлены в `contracts/coverage-api.md` и
`contracts/collection-state-api.md`.

**Регрессия и наблюдаемый результат до/после:** backend-контракт одним сценарием проверяет
старый успех, подтверждённый новый успех, реальный отказ и отсутствие истории. Frontend
проверяет отдельные состояния `Нужна проверка` и `Ошибка`, частичный итог журнала и рост
числа запросов ко всем трём представлениям после завершения прогона. Быстрый общий гейт
`scripts/check.sh --no-docker` прошёл: backend — `876 passed`, эмулятор — `61 passed`,
frontend — все Vitest-тесты, сборка и статика; ruff, format, mypy и миграции также прошли.

**Open Design:** сначала обновлён канонический артефакт `market-data.html` в активном
проекте Open Design: добавлены частичный итог журнала и отдельные состояния старого
непроверенного и подтверждённого справочника. Проверки артефакта дали `26 checks passed`,
проверка ширины — `PASS 375=375`. MCP-клиент ожидал локальный HTTP daemon на порту 7456,
тогда как установленный Open Design 0.16.1 работал через named-pipe IPC; поэтому тот же
активный проект обновлён через его backing files и проверен штатными скриптами проекта.

**Живая сверка / сравнение БД:** внешних обращений и ремонта пользовательской БД не было;
состояния доказаны контрактными и UI-регрессиями на изолированных данных.

**Ограничения и невыполненные пункты приёмки:** единственный владелец запуска, конфликт
до HTTP, восстановление после рестарта и эксплуатационные бюджеты относятся к T224–T226.
Полный `scripts/check.sh` был начат, но остановлен по явному указанию владельца проекта до
завершения backend pytest; результатом приёмки остаётся зелёный `scripts/check.sh --no-docker`.

**Ссылка на факт, позволяющий поставить отметку:** legacy-успех без evidence v2 выдаёт
`requires_audit=1`, новый успех — `status=ok`, реальный отказ — `status=failed`; UI
показывает эти состояния раздельно и после завершения прогона повторно запрашивает три
независимых представления.

## T224 — единственный владелец всех путей сбора

**Статус:** выполнено.

**Ревизия или точный рабочий diff:** новый `market_data/lock.py`; внешние точки запуска в
`scheduler.py`, `runner.py`, `repair_audit.py` и `cli.py`; PostgreSQL-регрессии
`test_collection_lock.py`.

**Решение:** отдельная session-level PostgreSQL advisory-lock `(817241, 3)` удерживается
выделенной сессией до конца ежедневного цикла, ручного догона/продолжения, ремонта,
ручного односессионного запуска или первичной загрузки. Рабочая сессия может коммитить
пачки, не освобождая владение. Конфликт возникает до планирования, HTTP и изменения
состояния; UI получает существующий `409 catchup_already_running`, CLI — явный текстовый
отказ. Старые `running` помечаются `interrupted` только владельцем ежедневного запуска.

**Регрессия и наблюдаемый результат:** `ruff` и `mypy` для затронутых файлов прошли;
целевой набор runner, scheduler, backfill и review rules дал `33 passed, 84 skipped` в
локальном fallback-окружении. Новый PostgreSQL-набор `test_collection_lock.py` и
контракт ручного догона прошёл: `22 passed in 8.05s`; он проверяет отказ второго
владельца, то, что commit рабочей сессии его не снимает, и HTTP-конфликт ручного пути.
Полный `scripts/check.sh`
запускался как критическая правка: первая попытка остановилась на форматировании,
форматтер применён; две следующие были прерваны из-за двух одновременно оставшихся
дочерних pytest-процессов и их deadlock в общем fixture truncate. Процессы остановлены,
одиночный повтор завершился, но используемый терминальный транспорт не вернул его
итоговый код. Поэтому итоговый полный гейт сознательно не объявляется пройденным и будет
подтверждён на T233.

**Ограничения и невыполненные пункты приёмки:** проверки точных элементов, бюджета и
холодного рестарта ремонта остаются T225–T226; финальный полный гейт, живые сверки и
визуальная приёмка — T232–T233.

**Ссылка на факт, позволяющий поставить отметку:** все пять внешних путей используют один
`MarketDataRunLock`, а его ключ отличается от синхронизации счёта и Daily ML; освобождение
в `finally` сохраняет возможность следующего запуска после ошибки.

## T225 — точный и идемпотентный план ремонта

**Статус:** выполнено.

**Что изменено:** `market_repair_plan` хранит версию правила покрытия; завершённый план
возвращает сохранённый итог до создания клиентов. Незавершённые строки предварительно
сверяются по точной паре, поэтому данные, сохранённые перед аварией до отметки строки,
не вызывают повторный HTTP. Выполнение передаёт в `catch_up` по одной сохранённой паре,
а не декартово произведение всех дат и источников; закрытая пара получает отдельную
постоянную отметку.

**Проверка:** `ruff check`, `ruff format --check`, `mypy` затронутых модулей и
`pytest tests/market_data/test_repair_plan.py -q` прошли (`1 passed`). Полный гейт и
`--no-docker` не запускались по указанию владельца: полный гейт остаётся T232.

**Ограничения:** бюджет всех физических попыток, мягкая остановка и холодный рестарт
проверяются T226; рабочая БД не изменялась.

## T226 — бюджет и мягкая остановка ремонта

**Статус:** выполнено.

**Что изменено:** `repair-run` обрабатывает `Ctrl+C` как мягкую остановку: сигнал
передаётся в ISS, форму позиций и запросы ЦБ, уже сохранённые данные остаются, а
непройденные пары остаются pending. Бюджет резервируется до физической попытки и не
сбрасывается; повтор требует явного `repair-run`, а увеличение предела — отдельного
`repair-extend`.

**Проверка:** `ruff check`, `ruff format --check`, `mypy` затронутых модулей и
целевые `pytest tests/market_data/test_repair_plan.py tests/market_data/test_iss_client.py -q`
прошли (`37 passed`). Общий гейт намеренно не запускался: он зарезервирован для T232.

**Ограничения:** тесты подтверждают остановку до открытия клиентов и существующее
резервирование каждой попытки; живой ремонт и холодный процесс с рабочей БД не запускались.

## T228 — единый обход доски для котировок и агрегатов

**Статус:** выполнено.

**Что изменено:** котировки и агрегаты запрашивают единый набор колонок TQBR, который
кэшируется только на срок клиента и дату сессии. Обе записи и оба независимых исхода
источников сохраняются как прежде, но второй источник не начинает новую пагинацию.

**Проверка:** новый unit-сценарий подтверждает один физический `fetch_session_rows` и
два результата по строке; с `test_ingest_cycle.py` получено `1 passed, 16 skipped`
(PostgreSQL-нужные сценарии пропущены из-за локальной конфигурации порта). Ruff и mypy
затронутых модулей прошли. Общий гейт не запускался до T232.

## T229 — измеренный предел Brent fallback

**Статус:** выполнено без изменения торгового смысла ряда.

**Живая сверка:** `verify-brent --session 2026-03-16` получил 942 исторические строки
Forts за 10 HTTP-страниц, без повторов, за 1.25 с; фронтальный контракт — `BRJ6`,
закрытие `102.39000`. Исторический каталог, который позволил бы адресно запросить
контракт заранее, в доступном ISS-контракте не подтверждён. Поэтому fallback сохранён:
его замена эвристикой изменила бы правило `BRENT_FRONT`, а не была бы оптимизацией.

## T230 — read-only аудит и граница следующего ремонта

**Статус:** выполнено, данных не записывалось.

**Команда:** `repair-audit --from 2026-09-15 --till 2026-09-18 --source brent`.
На сохранённом календаре обнаружены четыре точные неизвестные пары `brent` ×
2026-09-15…2026-09-18: значения присутствуют, но evidence текущей версии отсутствует.

**Конкретный план для возможного T231:** только эти 4 даты и источник `brent`; верхняя
оценка — 10 страниц ISS на дату по T229, поэтому HTTP-бюджет — 40. Это не разрешение
на запуск: выполнение требует явного подтверждения владельца именно для этой области и
предела. Другие источники, даты и автоматическое расширение в план не входят.

## T227 — устранён повторный расчёт сводки

**Статус:** выполнено.

**Что изменено:** `build_report()` рассчитывает `requires_audit` для каждого источника
группы один раз и передаёт то же множество в раскрытие источников; `coverage_boundary`
также берётся один раз на весь отчёт. Семантика статусов и чисел не менялась.

**Сопоставимое измерение:** на одной локальной PostgreSQL и `asof=2026-09-18`, после
одного прогрева, отдельный вызов исходной ревизии занял 5.481239 с, оптимизированной —
4.710545 с (примерно 14% быстрее). Это локальная относительная величина, не обещание
latency другого сервера.

**Проверка:** ruff, format, mypy и `test_coverage.py` вместе с контрактом coverage
прошли: `29 passed in 8.58s`. Общий гейт не запускался и остаётся T232.

## T231 — разрешённый ремонт Brent

**Статус задачи:** выполнено по принятому критерию внешнего ограничения.

**Code / repair execution:** completed.

**Data completeness:** incomplete due to proven ISS contract limitation.

Создан и выполнен план `8d62b41a-af34-48ea-9e78-78426873ac36` ровно для `brent` ×
2026-09-15…2026-09-18 с разрешённым пределом 40. Расход достиг ровно 40, попыток
сверх предела не было; все четыре элемента остались pending.

**Причина:** ISS на первых трёх датах после страниц 0…1025 отдаёт пустую страницу
`start=1026`, одновременно объявляя курсор `TOTAL=1136`; на четвёртой дате бюджет
закончился на странице 300. Прямая повторная проверка того же адреса подтвердила
пустой `history.data` при `history.cursor=[[1026,1136,100]]`. Это противоречивый
ответ источника, а не основание объявить хвост собранным. Предыдущая оценка T230
(10 страниц на дату) оказалась недостаточной для этого изменившегося каталога.

**Продолжение по разрешению:** 2026-09-15…17 оставлены `pending` с причиной
`iss_contract_cursor_inconsistency` и не переспрашивались. Для 2026-09-18 предел
увеличен только до 52 (40 уже израсходовано + 12 страниц до проверяемой границы);
выполнено 11 попыток, на `start=951` воспроизведён пустой ответ при `TOTAL=1063`.
План теперь `incomplete`, расход 51/52, все четыре пары `unknown/pending` с причиной
`iss_contract_cursor_inconsistency`. Повторные HTTP для них не требуются по решению
владельца; это не превращает пустой ответ в подтверждённое отсутствие.

**Read-only сверка:** audit четырёх дат подтверждает `present=true, unknown=true`;
summary показывает Brent `failed` с четырьмя конкретными cursor-причинами, а не
полноту. Evidence/coverage для этих единиц не присваивались.

**Журнал:** прогон `8d62b41a-af34-48ea-9e78-78426873ac36` содержит четыре неуспешные
попытки с правильными датами и причинами cursor inconsistency. Это история исполнения;
сами четыре единицы плана остаются pending и не исчезают.

**ML readiness и materialized dataset:** readiness на 2026-09-18 — `ready=false`,
в missing groups присутствует `global`. Набор материализован по абсолютной ссылке
`file:///C:/datasets/2026-09-18-d83a6be0ea4731d1`; каждая из четырёх дат содержит
`brent` в `incomplete`, поэтому `dataset_is_complete=false`. Два последовательных
построения дали один digest
`sha256:d83a6be0ea4731d1eb04fcaeac7df79f1a97af35fb8500fb56aa36faaf1bf311`.
Evidence Brent по разрешённым датам — 0. Completed-единиц в этом плане нет, поэтому
проверка их повторного запуска неприменима; ни одна pending-единица успехом не объявлена.

**Исправленный сопутствующий дефект:** относительный `MARKET_DATA_DATASET_ROOT` теперь
нормализуется в абсолютный путь перед формированием URI. Целевой набор тестов dataset —
`23 passed`; ruff и mypy прошли. T232 до этой фиксации не запускался.

## T232 — общая и живая приёмка

**Код и миграции:** на рабочем дереве после `04b7e1b` полный
`scripts/check.sh` завершился с exit code 0. Backend: `883 passed` за 3:29;
alembic применил миграции `0001`…`0018` к чистой БД; Daily ML emulator:
`61 passed`; frontend: `235 passed`; все четыре Docker-образа собраны. До
этого гейт нашёл регрессию общего TQBR-обхода: при `IssError` второй потребитель
доски повторял физический запрос. Кэш теперь сохраняет и повторно передаёт этот
отказ в пределах клиента и даты; целевые сценарии и новый регрессионный тест
прошли (`4 passed`), затем повторён полный гейт.

**Read-only живые сверки (2026-09-22):**

- `verify-positions --session 2026-06-04 --ticker SBER`: `SBER → SBRF_F`;
  физические позиции 200902 / 53048, юридические 107597 / 255451.
- `verify-positions --session 2026-08-24 --ticker SBER`: та же семья `SBRF_F`;
  физические 125148 / 96720, юридические 394743 / 423171.
- `verify-brent --session 2026-03-16`: 942 строки, 10 успешных ISS-страниц,
  `BRJ6`, закрытие `102.39000`.
- `verify-cbr --session 2026-09-16`: ставка `14.00`, 12 точек ZCYC, в том
  числе `yield_1y`, `yield_2y`, `yield_5y`, `yield_10y`.
- `verify-reference --session 2026-09-18`: 44 веса IMOEX, 288 имён индексов,
  105 строк по отраслевым индексам.

**Визуальная приёмка: открыта.** Локальный compose-стенд был поднят, но в
сессии отсутствует доступный браузер: встроенный браузер сообщил
`Browser is not available: iab`. Поэтому сценарии ошибки единственного
источника, частичного сбора, остановки/продолжения, рестарта, непроверенного
справочника и готового повтора, а также сравнение с Open Design на обычной
ширине и 390 px не выполнены. Успех frontend-тестов не выдаётся за визуальную
проверку. T232 остаётся открытой до ручной браузерной приёмки.

## T233 — итог по замечаниям review-2026-09-21

| Замечание | Исправление и доказательство | Итог |
|---|---|---|
| R1 | T220–T221 различают отсутствие, ошибку и частичный остаток позиций; живые SBER-сверки выше подтверждают семью и значения. | Код и live check пройдены; UI открыт. |
| R2 | T216 и T219 валидируют контракт ответа и создают coverage только по проверенной единице; полный гейт прошёл. | Код пройден; UI открыт. |
| R3 | T217 покрывает серверный лимит пагинации; Brent-сверка прошла 10 страниц и 942 строки. | Пройдено. |
| R4 | T222–T223 сохраняют точный итог и передают его в UI; backend и frontend-тесты прошли. | Код пройден; UI открыт. |
| R5 | T224 блокирует второго владельца до HTTP, T225–T226 сохраняют план и бюджет. | Регрессии полного гейта пройдены; live-конкуренция не запускалась. |
| R6 | T225–T226 не повторяют completed-план и сохраняют pending при остановке. | Регрессии полного гейта пройдены; реальный completed-план отсутствовал в разрешённом ремонте. |
| R7 | T227 устраняет повторный аудит; измерение 5.481239 с → 4.710545 с зафиксировано выше. | Пройдено на сопоставимом локальном срезе. |

Ограничения T231 (четыре Brent-пары `unknown/pending` из-за противоречивого
cursor ISS) сохраняются. Визуальная приёмка T232 также сохраняется открытой;
поэтому T233 не закрывает фичу и не меняет отметки задач. Старые отчёты остаются
историей; актуальный вывод находится в разделах T232–T233 этого файла.
