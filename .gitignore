# Vendored agent skills.
# Third-party content installed from skills-lock.json; reinstallable, not project source.
.agents/skills/
.claude/skills/

# Machine-local agent settings.
.claude/settings.local.json

# Environment and secrets — never commit real credentials.
.env
.env.*
!.env.example
*.pem
*.key

# Logs, build artifacts, temporary files.
*.log
logs/
dist/
build/
tmp/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Node
node_modules/
coverage/
.vite/

# OS
.DS_Store
Thumbs.db

# Конфигурация локальных MCP-серверов: содержит абсолютные пути к приложениям
# на конкретной машине разработчика.
.mcp.json

# Файлы контекста конкретных AI-инструментов. Правила проекта живут в
# AGENTS.md и .specify/memory/constitution.md — они не привязаны к вендору.
CLAUDE.md
.cursorrules

# Исследовательский репозиторий Daily ML (MR-MASTER-DRO) — отдельный проект со
# своей историей, подключён локально как источник истины о модели ранжирования.
# Его код и данные не являются содержимым этого репозитория.
Daily ML/
